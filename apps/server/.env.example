
# 灰度开关：允许未携带访客令牌的请求读取会话详情与 SSE。
# 默认 false。会话读取需要出示服务端在对话响应里下发的访客令牌
# （REST 走 x-visitor-token 头，SSE 走 ?vt= 查询串）——否则知道 sessionId
# 就能读到完整对话，而 sessionId 是浏览器端生成的、会出现在 URL 与日志里。
# 仅在线上还有旧版 widget（不会带令牌）时临时置 true，全部升级后应关闭。
ALLOW_ANON_SESSION_READ=false
