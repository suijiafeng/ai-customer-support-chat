# Environment
# .env 是安全兜底默认值，可提交；真实配置写在 .env.local（不提交）
.env.local
.env.*.local

# Dependencies
node_modules/

# Logs
npm-debug.log*
*.log

# OS
.DS_Store
Thumbs.db

# Build
dist/
build/
vite.config.js.timestamp-*.mjs

# 本地 SQLite 数据库文件（含 WAL/SHM）
*.db
*.db-wal
*.db-shm

# 自动生成并持久化的 JWT 签名密钥（未显式配置 AUTH_SECRET 时）
.auth_secret
